# 上位机通信协议（串口）

本文档描述本项目固件的串口命令集与返回格式，供上位机（PC/手机/脚本）开发与联调使用。

## 1. 传输层

- 物理链路：USB 转串口（例如 CH340），连接到 ESP8266 的 UART0。
- 波特率：115200
- 数据位/校验/停止位：8N1
- 编码：ASCII 文本（不使用二进制帧）
- 行结束：以 `\r` 或 `\n` 结束一条命令（推荐发送 `\r\n`）。
- 最大命令长度：`USB_CMD_MAX_LEN`（当前为 64 字节，不含结尾 `\0`）。超长会被丢弃并清空缓冲。

## 2. 回显（Echo）

为方便人工使用串口终端调试，固件支持“输入回显”：设备将收到的字符原样回写。

为避免影响上位机在握手阶段做严格解析，当前固件的行为是：

- 回显仅在“已连接会话（CONNECT 成功后）”期间启用；握手阶段（HELLO/CONNECT）不回显。
- 上位机可运行时动态控制：`SET:ECHO=0/1`。

编译期开关（PlatformIO）：在 `platformio.ini` 的 `build_flags` 添加：

- `-DFEATURE_USB_ECHO=0`（彻底关闭回显功能；此时 `SET:ECHO` 不再产生实际回显）

## 3. 连接与心跳模型

固件内部维护“连接”状态（`is_connected`），并在超时后自动退回到 AUTO（关闭外部覆盖）。

- 建议流程：
  1. 上位机发送 `HELLO` 获取设备标识
  2. 上位机发送 `CONNECT` 表示开始会话
  3. 会话期间每隔 1~2 秒发送一次 `PING`（或周期性发送 `GET:STATUS`）作为 keep-alive

- 配对超时：`HELLO` 后超过 `USB_PAIRING_TIMEOUT`（默认 3000ms）仍未 `CONNECT`，设备会回到 IDLE；此时需要重新 `HELLO`。
- 连接门禁：除 `HELLO/CONNECT/DISCONNECT` 外，其他命令在未连接时都会返回 `ERR:NOT_CONNECTED`。

- 心跳超时：超过 `USB_HEARTBEAT_TIMEOUT`（当前 5000ms）未收到任何命令，会自动断连并关闭覆盖。

## 4. 响应格式约定

- 所有响应均为单行文本并以 `\r\n` 结尾。
- 成功响应：以 `OK:` 开头，或返回特定关键字（如 `PONG`）。
- 错误响应：以 `ERR:` 开头。

## 5. 命令清单

### 5.1 基础命令

- `HELLO`
  - 返回：`DEVICE:ESP8266_FAN_CTRL_V1.0`

- `CONNECT`
  - 返回：`OK:CONNECTED`
  - 备注：允许幂等调用；已连接时再次 `CONNECT` 会刷新心跳并返回 `OK:CONNECTED`。

- `DISCONNECT`
  - 返回：`OK:DISCONNECTED`
  - 行为：主动结束会话并清除外部覆盖（回到自主控制）。

- `PING`
  - 返回：`PONG`

- `RESET`
  - 返回：`OK:RESET`
  - 行为：清除外部覆盖、清除瞬态告警并重置内部状态；不会自动解除“电压异常锁定”。
  - 说明：若当前已锁定输出（例如电压异常/多次堵转触发），`RESET` 只会保留该锁定相关错误位；需要排查硬件原因后再通过专用方式解锁（或重上电）。

- `UNLOCK`
  - 返回：`OK:UNLOCK` 或 `ERR:UNLOCK_DISABLED`
  - 行为：请求解除输出锁定（仅用于调试）。
  - 说明：该命令默认关闭，需在固件编译期启用 `FEATURE_HOST_UNLOCK_OUTPUT=1` 才会生效；若硬件异常仍在，解锁后可能很快再次触发锁定。

### 5.2 查询命令

- `GET:STATUS`
  - 返回：一行 JSON（示例）
    ```json
    {"conn":1,"locked":0,"manual":0,"ov_pwm":0,"ov_v":0,"ov_rpm":0,"echo":1,"temp":24.06,"volt":0.16,"target":12.00,"rpm":0,"target_rpm":800,"pwm":0,"error":"0x0","mode":"AUTO"}
    ```
  - 字段说明：
    - `conn`：是否已连接（1=CONNECT 成功后；0=未连接）
    - `locked`：输出是否处于锁定保护（1=已锁定，固件会强制拉到安全输出；此时 `target/pwm` 可能不再体现“期望目标”）
    - `manual`：是否处于上位机手动模式（1=MODE=MANUAL；注意未必真的覆盖输出）
    - `ov_pwm` / `ov_v` / `ov_rpm`：当前是否启用 PWM/电压/RPM 覆盖
    - `echo`：当前回显是否启用（仅在连接态有效）
    - `temp`：当前温度（℃）
    - `volt`：当前电压估计值（V，受硬件采样/标定影响）
    - `target`：目标电压（V）
    - `rpm`：当前转速（RPM）
    - `target_rpm`：目标转速（RPM，自主模式由温度映射产生；手动 RPM 覆盖时为上位机目标）
    - `pwm`：当前 PWM 占空比（0~255）
    - `error`：错误标志（十六进制字符串）
    - `mode`：`AUTO` / `MANUAL`

  #### error 错误码位（ErrorFlags）

  `error` 字段是按位或（bitwise OR）的错误标志，格式为十六进制字符串（例如 `"0xA"`）。当前固件定义如下：

  - `0x00`：ERROR_NONE，无错误
  - `0x01`：ERROR_TEMP_SENSOR，温度传感器读取失败（DS18B20 异常/未接好等）
  - `0x02`：ERROR_FAN_STALLED，风扇堵转/转速异常（RPM 长时间为 0 或低于阈值）
  - `0x04`：ERROR_OVERHEAT，超温（达到/超过 TEMP_OVERHEAT）
  - `0x08`：ERROR_VOLTAGE_ABNORMAL，电压异常（实测电压低于目标一定阈值，触发保护锁定输出）
  - `0x10`：ERROR_USB_TIMEOUT，USB 心跳超时导致断连（通常会在“下一次连接后的首次状态上报”中出现一次）

  示例：

  - `0x0A` = `0x02`(堵转) + `0x08`(电压异常)

  补充说明（保护去抖/武装）：

  - `ERROR_VOLTAGE_ABNORMAL` 不是单次采样就触发：固件会在上电宽限、以及每次改变输出后的稳定时间之后，连续多次检测到“实测电压显著低于目标”才会锁定输出。
  - 为避免低电压/停转区间误判，固件仅在目标电压达到一定水平时才会武装电压异常锁定（默认阈值由 `VOLTAGE_ABNORMAL_ARM_MIN_TARGET` 控制）。
  - `ERROR_FAN_STALLED` 只会在固件认为“正在尝试起转”（目标电压进入可起转区间，并且过了起转宽限）时才会触发，避免低温/低压或刚上电时的误判。

  补充说明（闭环步进限制）：

  - 自主模式与 RPM 覆盖模式下的 PI 调压带有“单次最大调压步进”限制（`RPM_CTRL_MAX_V_STEP`），用于避免 PWM 突变导致供电掉压/采样瞬态。

- `GET:INFO`
  - 返回：一行 JSON（示例）
    ```json
    {"device":"ESP8266_FAN_CTRL","fw":"V1.0","proto":1}
    ```

- `GET:CONFIG`
  - 返回：一行 JSON（包含关键运行/标定参数，便于上位机自检；字段可能随固件演进增加）

- `GET:OVERRIDE`
  - 返回：一行 JSON（当前覆盖开关与目标值）

- `GET:ECHO`
  - 返回：`OK:ECHO=0/1`

- `GET:MODE`
  - 返回：`OK:MODE=AUTO` 或 `OK:MODE=MANUAL`

### 5.3 设置命令（SET）

SET 命令格式为：`SET:<KEY>=<VALUE>`。

- `SET:MODE=AUTO`
  - 返回：`OK:MODE=AUTO`
  - 行为：关闭外部覆盖，恢复温控自动调节。

- `SET:MODE=MANUAL`
  - 返回：`OK:MODE=MANUAL`
  - 行为：进入手动模式（需要配合 `SET:VOLTAGE` 或 `SET:PWM` 实际控制输出）。

- `SET:VOLTAGE=<float>`
  - 返回：`OK:VOLTAGE=<值>` 或 `ERR:VOLTAGE_OUT_OF_RANGE`
  - 行为：设置目标电压并进入外部覆盖（MANUAL）。

- `SET:PWM=<int>`
  - 范围：0~255
  - 返回：`OK:PWM=<值>` 或 `ERR:PWM_OUT_OF_RANGE`
  - 行为：直接设置 PWM 占空比并进入外部覆盖（MANUAL）。
  - 备注：当 PWM 覆盖开启时，优先级高于 `SET:VOLTAGE`。

- `SET:RPM=<uint>`
  - 返回：`OK:RPM=<值>` 或 `ERR:RPM_OUT_OF_RANGE`
  - 行为：启用“RPM 覆盖”并进入外部覆盖（MANUAL）；固件会按目标 RPM 做闭环调压。
  - 备注：覆盖优先级为 `PWM 覆盖 > 电压覆盖 > RPM 覆盖`。

- `SET:TEMP_MIN=<float>` / `SET:TEMP_MAX=<float>` / `SET:TEMP_OVERHEAT=<float>`
  - 返回：`OK:TEMP_MIN=<值>` / `OK:TEMP_MAX=<值>` / `OK:TEMP_OVERHEAT=<值>`
  - 行为：上位机可运行时修改温控阈值；固件在收到后会立即应用到温控映射与超温判定。

- `SET:ECHO=0/1`
  - 返回：`OK:ECHO=0/1`
  - 行为：运行时开关回显（仅在编译期启用 `FEATURE_USB_ECHO` 时生效）。

- `SET:CLEAR_OVERRIDES=1`
  - 返回：`OK:CLEAR_OVERRIDES`
  - 行为：清除所有外部覆盖标志（等价于回到 AUTO），但不主动断开连接。

- `SET:UNLOCK=1`
  - 返回：`OK:UNLOCK` 或 `ERR:UNLOCK_DISABLED`
  - 行为：与 `UNLOCK` 等价（仅用于调试）。

## 6. 推荐上位机实现要点

- 始终按行读取（遇到 `\r`/`\n` 结束一条消息），不要按固定长度读。
- 建议握手后再发送 GET/SET；未连接时除 `HELLO/CONNECT/DISCONNECT` 外都会返回 `ERR:NOT_CONNECTED`。
- 若你启用了回显：建议上位机默认 `SET:ECHO=0`，避免与严格解析冲突。
- 建议维护 keep-alive（每 1~2 秒 `PING`），避免 5 秒超时导致设备自动退回 AUTO。
- 串口繁忙时优先发送一条命令并等待响应，避免多条命令粘包导致调试困难。
