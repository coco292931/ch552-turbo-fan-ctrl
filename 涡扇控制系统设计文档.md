
---

# 涡扇控制系统设计文档

## 基于 FB 电流注入的 Fail-Safe 风扇 DC 调速方案

**（XL4015 + ESP8266 反馈偏置型电压调节系统）**

---

# 1. 项目概述

本项目用于实现 12V 直流风扇的智能调速控制与状态监测。
系统采用 XL4015 作为主功率调节单元，ESP8266 作为主控制器，通过对反馈（FB）节点进行电流注入实现输出电压可控下调。

系统具备以下特性：

- 输出电压可调
- 风扇转速闭环控制
- 温度监测
- USB 双向通信
- Fail-Safe 默认安全输出机制
- 异常保护与自动恢复机制

---

# 2. 系统组成

## 2.1 核心器件

| 模块       | 器件                    |
| ---------- | ----------------------- |
| 主控芯片   | ESP8266                  |
| 降压控制器 | XL4015                  |
| 温度传感器 | DS18B20                 |
| 风扇类型   | 3Pin DC 或 4Pin PWM风扇 |

## 2.2 电源结构

- DC 12V：风扇供电
- DC 5V：主控与外围逻辑供电

---

# 3. 控制原理

## 3.1 电压调节机制

XL4015 内部误差放大器将 FB 引脚电压与内部 1.25V 参考电压进行比较。

标准反馈网络：

```bash
Vout — 13kΩ — FB — 1.5kΩ — GND
```

当 FB 电压低于 1.25V 时，输出电压提升；
当 FB 电压高于 1.25V 时，输出电压降低。

### 调节方式

ESP8266 输出 PWM 信号，经 RC 低通滤波后形成模拟电压，并通过串联限流电阻注入 FB 节点，实现输出电压可控下调。

控制逻辑：

- PWM = 0% → 输出 = 12V（硬件默认值）
- PWM 增大 → FB 电压升高 → 输出降低
- MCU 失效或高阻 → 输出恢复至 12V

该结构保证：

> MCU 仅具备削弱输出能力，不具备提升输出能力。

系统默认状态为硬件设定最大电压，具备 Fail-Safe 特性。

---

## 3.2 低通滤波参数

- 限流电阻：20kΩ
- 滤波电容：0.1µF
- 截止频率约 30Hz

设计原则：

- 截止频率远低于 PWM 频率（最高 375kHz）
- 避免 PWM 纹波进入反馈环
- 控制变化速度低于电源环带宽

---

## 3.3 电压检测

输出电压通过分压网络检测：

```bash
Vout — 10kΩ — ADC — 5.1kΩ — GND
```

当输出为 12V 时：

ADC 电压 ≈ 4.05V

由 ESP8266 ADC 进行采样。

---

## 3.4 风扇转速检测

- 使用 GPIO4（见 [src/config.h](src/config.h)）
- 支持开漏信号输入（通常需上拉）
- 可选外部上拉电阻
- 使用定时器或中断计数计算 RPM

---

## 3.5 温度检测

- 使用 GPIO5（见 [src/config.h](src/config.h)）
- 连接 DS18B20
- 单总线协议
- 外接上拉电阻

---

# 4. 引脚分配

引脚以 [src/config.h](src/config.h) 为准，下表为当前默认配置。

## 4.1 ESP8266（NodeMCU v2）

| 引脚 | 功能 |
|---|---|
| GPIO13 | PWM 输出（注入 XL4015 FB） |
| A0 | 输出电压 ADC 检测 |
| GPIO4 | 风扇转速检测（FG） |
| GPIO5 | 温度检测（DS18B20 单总线） |

---

# 5. 工作模式

系统包含两种模式：

## 5.1 自主控制模式（默认）

- 根据温度与转速进行闭环控制
- 不依赖串口通信

## 5.2 串口优先控制模式

- 上位机可设置目标参数
- 系统执行外部控制策略

---

# 6. 串口通信机制

说明：当前“上位机通信”采用 **UART 串口的文本协议**（通过 USB 转串口适配器接入），并非 USB HID/CDC 二进制包。
协议细节以 [上位机通信协议.md](上位机通信协议.md) 为准。

## 6.1 配对流程

推荐流程：

1. 上位机发送 `HELLO` 获取设备标识
2. 上位机发送 `CONNECT` 建立会话
3. 会话期间周期发送 `PING`（或 `GET:STATUS`）作为 keep-alive

---

## 6.2 数据交互

### GET 请求

- 当前温度
- 输出电压
- PWM 占空比
- 风扇转速
- 系统配置

### POST 请求

- 目标电压
- 温度阈值
- PWM 占空比或频率
- 风扇目标转速
- 其他系统参数

---

## 6.3 心跳机制

- 上位机周期发送 `PING` 或 `GET:STATUS`
- MCU 回复（`PONG` 或状态行）
- 超时（默认 5000ms）视为断连并退回 AUTO

---

# 7. 异常处理机制

## 7.1 风扇堵转

条件：

- 转速突降或为 0

处理：

1. PWM = 100%
2. 上报警告
3. 延时后尝试重启
4. 连续两次失败 → 锁定停转
5. 等待上位机指令

---

## 7.2 USB 断连

- 立即切换至自主模式
- 恢复连接后上报异常记录

---

## 7.3 超温保护

- 超过温度阈值 → 上报警告
- 上位机无响应 → 切换自主模式
- 温度恢复后允许恢复外部控制

---

## 7.4 输出异常 / 短路

检测到输出电压异常下降：

1. PWM = 100%
2. 锁定输出
3. 上报错误
4. 仅允许：
   - 手动断电重启
   - 上位机发送校验通过的复位命令

---

# 8. 失效模式分析（Fail-Safe）

| 情况     | 系统行为     |
| -------- | ------------ |
| MCU 掉电 | 输出恢复 12V |
| PWM 高阻 | 输出恢复 12V |
| USB 断连 | 切换自主控制 |
